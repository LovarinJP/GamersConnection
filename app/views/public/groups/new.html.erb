<div class="container">
  <%= render 'public/posts/error', post: @group %>
  <div class="row d-flex justify-content-center">
    <div class="col-6 col-12 text-center">
      <p class="pheader">グループ作成</p>
    </div>
  </div>

  <%= render 'form', group: @group %>
  
</div>
